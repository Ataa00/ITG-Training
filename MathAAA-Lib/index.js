
module.exports.add = function(a, b){ return a + b; };

//We addeed the function below so we have to update the version
//even maually inside package.json or using terminal "npm major-minor-patch" one of these three options. Then publish

module.exports.mult = function(a, b){ return a * b; };